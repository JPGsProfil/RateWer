ImageButton btn = (ImageButton) findViewById(cardList.Get(index1).viewID);
                                        btn.setAlpha(0.4f);
                                        btn.setClickable(false);

auslagern, mehrfach verwendet
Design